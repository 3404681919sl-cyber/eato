# `meal-ui` 规格

## 目标

把已完成的饭局领域、创建、决策、投票与历史能力组装成一个单设备可完成的饭局工作台：

```text
饭局列表 → 创建饭局 → 收集成员资料 → 生成规则方案 → 代入成员投票
  → 确认饭局 → 记录饭后结果 → 本地历史可回看/更正
```

本模块是第一阶段的可操作闭环，不新增真实身份、跨设备协作、外部餐厅数据、AI 或网络请求。

## 已确认边界

- 继续保留既有 Eato 的暖色视觉、顶栏和三项原页面；饭局工作台以增量入口加入，不重做全站导航。
- 所有成员都是同一设备中的模拟身份；页面明确标示“仅本设备”，不显示“已邀请”“已同步”等误导性文案。
- 读取与保存均通过 `MealEventRepository`；UI 不直接操作 `localStorage`。
- 规则推荐只调用既有 `rankMealOptions`，不调用 AI，也不将缺失距离/历史数据伪装成真实值。
- 本期候选为人工录入：名称、菜系标签、可选人均消费与备注。真实 POI、优惠和链接仍不属于本期。

## 信息架构与导航

1. 顶栏保留“发起饭局”按钮；成功创建后进入工作台并选中该饭局。
2. 在同一增量入口中提供“我的饭局”列表：按 `updatedAt` 倒序，显示饭局名称、状态、成员数、候选日期范围。
3. 选择一个饭局后打开“饭局工作台”；空状态引导用户发起第一场饭局。
4. 工作台只展示当前饭局，不混入旧打卡表、日历或数据分析的业务数据。

## 工作台阶段

### 1. 收集（`collecting`）

- 成员切换器：发起人可选择当前代填对象；屏幕中明确显示“正在代填：某成员”。
- 可用时间：当前成员可为候选日期添加或移除午餐、下午茶、晚餐；不要求所有成员存在完全交集。
- 饮食偏好：喜欢菜系、不喜欢菜系、忌口以可移除文本标签输入；预算预填饭局预算且可按成员调整；`isFlexible` 可切换。
- 人工候选：输入餐厅/菜系名称、菜系标签和可选人均消费；候选数据必须绑定当前饭局。候选编辑只在本地保存。
- 生成方案前要求至少有一个候选，且每名模拟成员至少有一个可用时间；没有完整时间交集仍允许进入决策，系统须显示协调冲突。

### 2. 决策（`deciding`）

- 调用 `rankMealOptions(event)` 显示不超过三项方案；每项展示候选、日期餐段、总分、分项理由与冲突提示。
- 显示缺少价格、距离或历史数据时的“中性分”说明；不得展示为外部实时数据。
- 成员切换器继续可用。当前成员可对每个候选投“支持 / 中立 / 否决”；否决可填写原因。
- 每次投票调用 `castVote`，成功后 `repository.save`；同成员同候选的后一次投票替换前一次。
- “确认方案”只对全员共同可用且无否决的方案可点击；执行 `confirmDecision` 后保存。其他方案显示不可确认的原因。
- 决策状态仍允许回到收集面板修改资料和候选，再重新计算前三方案；不自动确认。

### 3. 已确认（`confirmed`）

- 显示已选候选、日期、餐段、确认理由和确认时刻。
- 提供“重新协调”按钮，调用 `returnToCollecting` 并保存，保留过去投票和上次决定。
- 提供饭后记录表：是否成行、实际人均、1–5 分、备注。提交调用 `recordMealHistory` 并保存。

### 4. 已完成（`completed`）

- 展示饭后记录与完成时刻；允许更正花费、评分和备注，但不允许将已成行改回未成行。
- 禁用新增投票和确认按钮，并在文案上解释饭局已归档。

## UI 与可访问性

- 使用现有 semantic Tailwind token、圆角卡片、`lucide-react`；不新增 UI 库、字体或全局配色。
- 采用逐层披露：先显示当前阶段的主任务，辅助收集区折叠或次级呈现，避免一次堆满全部表单。
- 每个表单控件具备可见关联标签；错误紧邻字段并以 `role="alert"` 公布；保存、生成、投票、确认与记录都有加载/成功/失败反馈。
- 所有按钮使用原生 `button`，最小触控尺寸 44px；图标旁有文字，图标单独使用时提供 `aria-label`。
- 移动端单列、宽屏最多两列；不依赖 hover、不出现关键横向滚动。键盘焦点顺序与视觉顺序一致。

这些约束基于 UI/UX 检索结果：关联可见标签、渐进披露、明确提交反馈与触控可达性。

## 组件与数据边界

| 部分 | 责任 |
| --- | --- |
| `MealWorkspaceView` | 载入/选择本地饭局，展示阶段和保存反馈。 |
| `MealCollectionPanel` | 代填成员的时间、偏好和人工候选，生成不可变事件补丁后调用 Repository 保存。 |
| `DecisionOptionsPanel` | 调用规则引擎，展示前三方案，驱动投票和确认操作。 |
| `MealHistoryPanel` | 显示确认结果与饭后记录，驱动重新协调和历史更新。 |
| 领域模块 | 继续负责校验、状态和可解释评分；UI 不复制规则。 |

## 验收标准

1. 从“发起饭局”可进入/返回本地饭局工作台，并能再次打开已保存事件。
2. 可代入 3–8 名成员填写时间、偏好和人工候选；每次保存只影响当前饭局。
3. 决策页显示规则引擎的前三方案、理由与冲突；支持、否决、确认、重新协调和饭后记录均调用现有领域操作并持久化。
4. 无全员共同时间、忌口/预算硬约束、否决、缺少数据和完成状态都以明确可见的方式处理，不出现错误确认。
5. 原有三项 Eato 页面保持可访问和行为不变；新增 UI 有组件级交互测试、全量测试与生产构建验证。

## 不做的事

- 不做微信登录、邀请链接、真实成员权限、实时在线状态或跨设备更新。
- 不做地图搜索、抓取餐厅/优惠、实时价格，也不暴露任何密钥。
- 不做完整设计重构、动画系统或独立移动原生应用。
