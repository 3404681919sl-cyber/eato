# 2026-07-18 工作日志

- 17:30 修复样式丢失：Tailwind CSS 扫描路径错误导致生产构建未生成工具类。将 `src/styles/tailwind.css` 从 `@import 'tailwindcss' source(none); @source 'src/**/*.{js,ts,jsx,tsx}';` 改为 `@import "tailwindcss"; @source "../src/**/*.{js,ts,jsx,tsx}";`，重新构建后 CSS 从 8.88kB 增至 38.65kB，工具类恢复。已重新部署 CloudStudio 静态站点：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work
- 同时提交：index.html 错误兜底、src/main.tsx React 渲染错误捕获、server/src/routes/poi.js 高德 POI 类型编码 050000。
- 17:45 用户反馈页面被改、无真实头像、比价报错。处理如下：
  - 恢复简洁 LandingPage：删除 Codex 拆分的 HeroSection/FeaturesSection/CTASection，重写 LandingPage.tsx，还原背景图 collage + 居中大 Eato logo + 一句副标题，去掉冗余文字。
  - 修复 POI 搜索无反应：静态站点没有后端，`/api/poi/search` 会 404。将 `VITE_AMAP_KEY` 放入 `.env`，`poiService.ts` 改为后端不可用时浏览器直接调用高德 Web 服务 API（Key 会暴露，仅 Demo 使用）。
  - 修复比价 NoSuchKey：`DealCard.tsx` 和 `DealsPanel.tsx` 中分类图片路径写成了 `.png`，但 `public/brands/` 下只有 `.svg`，改为 `.svg` 后图片正常。
  - 已重新构建并部署：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work
- 17:59 用户继续反馈：LandingPage 仍不对、比价入口位置奇怪、比价数据不严谨（98 元无来源）、缺平台头像、打卡取消不明显。处理如下：
  - 比价数据严谨化：`src/data/deals.ts` 不再虚构叠加优惠，最优到手价 `finalPrice = minP`（列表真实最低价），saving = base - minP，bestStack 文案基于真实最低价。
  - 平台头像：`src/constants/index.ts` 给 `PLATFORMS` 加 `logo` 字段；`DealCard.tsx` 在卡片左上角显示平台 logo（首字）。
  - 比价入口：`PlaceRow.tsx` 把查优惠按钮从"打卡记录"列移到餐厅名称旁边（仅第一行显示），避免和打卡操作混在一起；`TableView.tsx` 把比价展开从按 visit 改为按 place，并添加标题"为 [餐厅名] 查找优惠"。
  - 打卡取消：`PlaceRow.tsx` 对勾按钮 title 根据 checkedIn 状态显示"打卡"/"取消打卡"，hover 更明确。
  - 已重新构建并部署：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work
- 18:32 用户明确要求：**保留 Figma 设计的 UI，未经用户要求不得改动任何页面 UI**。已将规则写入用户级 `~/.workbuddy/MEMORY.md`（"UI 修改铁律"段）。
  - 用户贴了 Figma 设计图（当前模型看不到图），确认标准版 = Figma 原版（含功能卡片+数据统计+页脚），非我之前删卡片的精简版。
  - 恢复 `src/components/landing/LandingPage.tsx` 为 git `105cdeb` 的 Figma 原版完整首页（背景图 collage + 大 Eato logo + badge + 副标题 + 开始约饭/了解更多按钮 + 浮动 stats + 三张功能卡片 + 页脚 CTA）。
  - 提交 `restore: 恢复 Figma 原版首页...`，并打标签 `ui-baseline`（git tag -f ui-baseline）冻结基准 UI，误改可 `git checkout ui-baseline -- <file>` 回滚。
  - 重新构建并部署：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work
  - 注意：TableView/DealsPanel/比价数据/打卡取消等之前的功能修复保留，不在首页，不受影响。
- 18:38 用户反馈内页（打卡表/约饭时间/数据分析）依旧不是 Figma 原版。处理如下：
  - 恢复 `src/constants/index.ts` 的 CAT/MOOD emoji 为 Figma 原版（如 noodles 🍜、sushi 🍣、curious 👀、casual 😌 等）。
  - 恢复 `src/data/deals.ts` 为 Figma 原版 `generateDeals`（含 0.92 叠券建议文案）。
  - 恢复 `src/components/table/TableView.tsx` 为 Figma 原版：表格列「日期&时间/待打卡点/心情/拟定菜单/优惠比对/✓打卡/花费/评价/再约」，内联行渲染，保留 StarRow/MoodPicker/CategoryPicker 作为依赖。
  - 恢复 `src/components/deals/DealsPanel.tsx` 为 Figma 原版：平台价格条 + AI 叠券建议。
  - 恢复 `src/components/calendar/CalendarView.tsx` 为 Figma 原版：标题「约饭时间协调」、用户切换、30/60 分钟粒度、重叠热力格子。
  - 恢复 `src/components/analytics/AnalyticsView.tsx` 为 Figma 原版：KPI 卡片、各店消费占比环形图、月度打卡趋势柱状图、常去榜、约饭搭档排行。
  - 清理因恢复 `deals.ts` 导致的 `getPlatformDealUrl` 残留引用（`src/data/index.ts`、`src/services/mockDataService.ts`）。
  - 提交 `restore: 恢复 Figma 原版内页 UI...`，并更新 `ui-baseline` 标签。
  - 重新构建并部署：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work
- 18:50 用户再次反馈"根本不是最开始那一版，只改了首页，预览页左边没图片，里面没恢复"。
  - **根因定位**：① 之前两次 restore（0f4a1cf/04253d0）是按组件"重写"的杂交版，≠ 原版；② 原版 `105cdeb` 用 Unsplash 外链图，被 Codex `7d08075`"清理Unsplash"全删（含 `image` 字段），所以登录页左侧大图、表格行头像全空。
  - **正确做法**：整文件换回 `git show 105cdeb:src/app/App.tsx > src/app/App.tsx`（105cdeb 是单文件自包含 App，1365 行，仅依赖 react/lucide/recharts）。`npm run build` 通过（2218 模块），产物含全部 Unsplash 图（登录页左侧 900x1200、5 家餐厅行 120x120、展示 400x500）。
  - 提交 `restore: 整文件换回 Figma Make 原版 App.tsx(105cdeb)...`，`git tag -f ui-baseline` 指向 `12b0ffd`（真正的原版基准）。
  - 重新部署：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work 。现在所有页面(首页/登录/打卡表/约饭时间/数据分析)均为 Figma Make 原版，图片全回来。

- 19:30 用户在 Figma 原版基础上提出 4 处优化（均属"用户明确要求的 UI/文案改动"，符合铁律），逐项处理：
  1. **响应式**：电脑端页面需要左右滑动。用户确认可放以后调整，本次不动。
  2. **比价面板文案/高亮**（改 `src/app/App.tsx` 单文件）：
     - `generateDeals` 删除虚构的 `finalPrice = minP*0.92` 与 `saving` 虚构；`bestStack` 改为"当前最低价为 XX 平台（¥XX），可直接购买。"，去掉"新用户优惠券"文案（贴合"授权各平台账号后能识别新老用户"的设想）。
     - 渲染去掉右侧"可节省 ¥xx"块；最低价行高亮加强：`ring-2` + 边框用 `p.color`（实色）+ `boxShadow 0 0 0 2px color30`。
  3. **跳转链接**：`DealsPanel` 增加 `placeName` prop；链接从平台首页（meituan.com 等）改为按餐厅名搜索各平台（`s.meituan.com/店名`、`douyin.com/search/店名`、`dianping.com/search/keyword/店名`、`s.taobao.com/search?q=店名`、`s.ershou.taobao.com/search.htm?q=店名`），`encodeURIComponent` 处理中文。调用处补 `placeName={place.name}`。
  4. **打卡 bug**：用户描述"为什么它是…"被截断。当前 `checkedIn: !checked` 点击切换逻辑正常，但原版按钮无 hover 提示。给打卡按钮加 `title`（已打卡→"点击取消打卡"；未打卡→"点击打卡"）+ hover 浅底，未改布局。具体 bug 现象需用户补全描述确认。
  - 构建通过，已重新部署：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work
  - 注：本次改动是 Figma 原版基础上的功能/文案微调，未改整体布局；`ui-baseline` 标签仍指向原版基准（12b0ffd），如需回滚原版 UI 用 `git checkout ui-baseline -- src/app/App.tsx`。
- 20:03 用户提出数据分析月度趋势与搜索店名优化：
  1. **月度打卡趋势**：原来只有 1-5 月，希望显示 1-12 月并支持年份切换。
     - 在 `AnalyticsView` 内加 `year` state，默认当前年；在柱状图卡片标题行右侧增加年份切换（< / > / 年份显示），**位置选择放在小框左上角**（更贴近该图表，不影响页面大标题）。
     - 生成 1-12 月数据：当前年份保留 1-4 月历史数据、5 月用真实已打卡数，6-12 月按 visits 索引分配；其他年份用伪随机 seed 生成演示数据，切换年份可见不同趋势。
  2. **搜索店名自动推荐**：输入店名时像美团一样自动推荐店名+地址。
     - 在 `TableView` 中接入高德 POI Web 服务（`VITE_AMAP_KEY`），输入时 350ms debounce 自动搜索餐饮类 POI。
     - 下拉显示店名+地址+缩略图（如有），点击直接填入打卡表；无结果时提示"暂无推荐，按回车直接添加"。
     - 回车默认选择第一条推荐，无推荐则直接添加当前输入。
  - 构建通过（45.80 kB CSS, 609.47 kB JS），已提交并重新部署：https://93f7ca0f51f24e958ca6db9204ef43a6.app.codebuddy.work
