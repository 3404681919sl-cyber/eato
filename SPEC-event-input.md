# `event-input` 规格

## 目标

在不改变现有 Eato 主体视觉和既有“打卡表 / 约饭时间 / 数据分析”能力的前提下，为第一阶段加入一个可用的饭局创建入口。发起人可在**一台设备、一个浏览器**中创建饭局，并代入 3 至 8 名模拟成员填写基础资料，创建完成后把完整的 `MealEvent` 通过 `MealEventRepository` 持久化到本地，进入 `collecting` 状态。

这不是多人实时协作、分享链接或微信登录的实现；这些能力留给第二阶段的身份与真实数据库模块。

## 已确认的产品约束

- 当前采用单设备模拟：发起人可代表成员填写资料、切换成员和查看彼此的时间、预算及忌口。
- 模拟成员数必须为 3 至 8 人；创建者属于其中一人。
- 现有 UI 保留，只以一个明确的“发起饭局”入口和独立创建页（或等价的受控面板）增量接入；不替换导航体系、不重做全站视觉。
- 不调用 AI、不读取真实餐厅库、不接入登录、不写入浏览器之外的服务。
- 所有饭局读取与写入必须经过 `MealEventRepository`，页面不得直接操作 `localStorage`。

## 用户流程

```text
现有 Eato 导航
  └─ 发起饭局
       ├─ 1. 填写饭局：名称、城市、区域、候选日期范围、人均预算
       ├─ 2. 填写模拟成员：3–8 名，第一名为发起人
       ├─ 3. 提交创建
       │    ├─ 输入不合法：保留已填写内容，并在对应字段旁显示中文错误
       │    └─ 输入合法：创建完整 MealEvent → repository.create → 成功反馈
       └─ 4. 进入收集阶段的饭局上下文（后续填写时间、偏好和候选）
```

## 输入与初始化规则

### 饭局基本信息

| 字段 | 类型 | 规则 | 初始值 |
| --- | --- | --- | --- |
| `title` | 文本 | 必填，使用既有 `validateMealDraft` 校验 | 空 |
| `city` | 文本 | 当前允许为空；未来 POI / 数据库接入时再收紧 | 空 |
| `area` | 文本 | 当前允许为空 | 空 |
| `candidateDateRange.start/end` | `YYYY-MM-DD` | 结束日期不得早于开始日期；至少提供一个可供选择的日期 | 当天与未来第 7 天 |
| `budget.min/max` | 非负整数 | 最低预算不得高于最高预算 | 50 / 100 |
| `participants` | 模拟成员列表 | 3–8 人，昵称非空且不能重复 | 创建者 + 2 个可编辑占位成员 |

### 创建出的领域对象

创建流程应生成一个完整且可被后续模块使用的 `MealEvent`：

- 使用稳定的客户端 ID；当前实现可用 `crypto.randomUUID()`，并为不支持的环境提供不依赖外部服务的安全降级策略。
- 第一个成员为 `role: "creator"`，其 ID 同时写入 `creatorId`；其余为 `role: "member"`。
- 初始 `status` 为 `collecting`；`availabilities`、`candidates`、`votes` 为空。
- 每位成员都生成默认 `PreferenceProfile`：喜欢、不喜欢、忌口为空，`isFlexible: true`，预算继承饭局预算。这样后续成员填写不用处理缺失资料。
- `createdAt` 与 `updatedAt` 为同一次创建时刻的 ISO 字符串。

## 交互与可访问性

- 页面继续采用现有暖色 token、圆角卡片和 `lucide-react` 图标，不新引入视觉库、字体或全局 token。
- 在小屏幕上使用单列、按步骤渐进展示；不依赖悬停，也不产生横向滚动。
- 每个输入都有可见标签，错误紧邻字段显示；验证在字段失焦时与提交时执行，提交后将焦点移至第一个错误字段。
- 提交按钮提供“创建中 / 已创建 / 创建失败”状态，防止双击重复创建；触控目标至少 44×44px。
- 成功后给出下一步提示，不把“本地模拟”伪装为已发送邀请或已同步给真实用户。

上述表单反馈规则来自 UI/UX 检索的已验证指导：关联可见标签、失焦校验、提交状态反馈。

## 模块边界

| 归属 | 内容 |
| --- | --- |
| 本模块 | 将表单草稿转换为已校验的 `MealEvent`；生成模拟成员与默认偏好；调用仓储创建；提供创建页/入口所需的状态与错误信息。 |
| `meal-domain` | 领域类型与基础业务校验。 |
| `event-store` | 本地读写、重复 ID 与存储错误的标准化处理。 |
| 后续 `meal-ui` | 饭局详情、成员切换、时间偏好填写、候选与决策结果的完整呈现。 |
| 不属于本模块 | 分享链接、真实身份、跨设备同步、AI 推荐、真实餐厅或优惠数据。 |

## 验收标准

1. 正常输入可创建 3–8 人的饭局，仓储中可取回内容完整、状态为 `collecting` 的事件。
2. 少于 3 人、多于 8 人、空名称、倒置预算、倒置日期范围和重复昵称都会阻止创建，并返回可显示的中文字段错误。
3. 创建失败时不清空用户输入、不产生半成品事件；成功创建仅调用一次仓储写入。
4. 现有三项 Eato 页面和其本地数据行为保持不变。
5. 对创建逻辑先写失败测试，再实现；最终运行相关单测、全量测试与生产构建。

## 不确定项与后续接口

- 创建成功后具体落到“饭局详情页”还是临时摘要页，将在 `meal-ui` 规格中确定；本模块只保证返回新建事件 ID 与创建结果，不预设复杂路由结构。
- 日期范围目前不处理时区、节假日或原生日历权限；真实协作阶段再单独设计。
