# `vote-and-history` 规格

## 目标

在单设备模拟饭局中，让发起人能代入每位成员对候选方案投票或否决；当选择已具备可确认时间且没有否决时，明确确认饭局；饭后再记录是否成行、实际花费、评分和备注。所有状态变化保持可复现、可存储，并且不绕过既有的领域规则和 `MealEventRepository`。

本模块只完成操作契约和本地状态保存；完整的饭局详情界面、成员切换与结果展示仍属于后续 `meal-ui`。

## 已确认范围

- 单设备模拟成员均可投票；成员之间可见时间、预算和忌口。
- 投票有 `support`、`neutral`、`veto` 三种值；同一成员对同一候选只保留一票，新操作覆盖旧票。
- `veto` 是硬信号：带有有效否决的候选不能被确认，且必须在结果中显示原因。
- 只有 `decision-engine` 标为 `readyToConfirm` 的候选时间方案才允许确认；没有全员共同时间时只能继续协调，不能自动确认。
- 确认后可退回 `collecting` 重新决策；已完成饭局不可再投票或再次确认。
- 饭后记录可补填；实际花费与评分必须是合理非负数，评分范围为 1–5。
- 不接入支付、真实出席、通知、AI、身份、分享或网络服务。

## 操作契约

### 投票

`castVote(event, vote)` 返回新的事件或 `DomainIssue[]`：

1. 仅 `collecting` / `deciding` 状态可投票。
2. `participantId` 必须属于饭局成员，`candidateId` 必须属于饭局候选。
3. 对同一“成员 × 候选”先删除旧票，再写入新票；不允许重复累积。
4. `veto` 可带原因；本期不要求原因必填，但 UI 将提示填写以帮助协调。
5. 成功时更新 `updatedAt`，不得修改传入事件。

### 确认

`confirmDecision(event, option)` 返回新的事件或 `DomainIssue[]`：

1. 仅 `deciding` 状态可确认。
2. `option.candidateId` 必须存在，且 `option.readyToConfirm` 必须为真。
3. 被任一成员 `veto` 的候选不能确认。
4. 成功时写入 `Decision`（候选、日期、餐段、规则理由、确认时刻），状态转为 `confirmed`。
5. 所有失败都保留原事件，不写入半成品 `decision`。

### 回退与饭后记录

- `returnToCollecting(event)` 仅在 `confirmed` 状态可用，复用 `transitionMealStatus` 的既有特例；保留历史票与上次决定，以便页面解释为何重新协调。
- `recordMealHistory(event, history)` 仅在 `confirmed` / `completed` 状态可用；首次成功时状态进入 `completed`，写入 `history.completedAt`；之后允许更正记录但不允许清空“已发生”事实。
- `history.occurred: false` 可记录未成行原因；当实际成行时 `actualSpend` 可填为 0 或正数，`rating` 仅允许 1–5 的整数或半星规则（本期采用整数）。

## 模块边界

| 归属 | 内容 |
| --- | --- |
| 本模块 | 纯状态操作、输入校验、投票去重、否决约束、确认和饭后记录；通过仓储保存操作结果。 |
| `decision-engine` | 生成候选时间方案及 `readyToConfirm`；不负责写入投票或决定。 |
| `event-store` | 读取与保存整个饭局事件。 |
| 后续 `meal-ui` | 呈现候选、代入成员、操作按钮、理由、历史表单与成功反馈。 |
| 不属于本模块 | 重新计算评分、自动替用户投票、跨设备冲突合并、真实出席核验、支付。 |

## 验收标准

1. 合法投票创建或替换一张票，且不会修改原事件；非法成员、候选或状态得到中文字段错误。
2. 否决候选不能确认；无全员共同时间的方案不能确认；合法方案可从 `deciding` 进入 `confirmed` 并记录决定。
3. 确认后可退回收集阶段；完成后不可投票或确认。
4. 饭后记录校验实际花费与 1–5 分评分，记录成功后进入 `completed`，后续允许更正内容。
5. 操作服务不读取 `localStorage`；由调用层显式传入仓储保存。先写失败测试，最终跑全量测试与生产构建。

## 开放项

- 投票、确认与饭后表单的具体页面布局留给 `meal-ui` 规格；本模块提供可直接驱动页面的操作结果与错误。
- “未成行”的原因是否细分为取消、改期、无人有空等枚举，待有真实使用反馈后再决定；本期用可选备注即可。
